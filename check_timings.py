from automation.email_digest import load_env; load_env(); from dashboard.gateway import get_connection; c = get_connection().cursor(); c.execute('SELECT timestamp, created_at, updated_at, status FROM prediction_ledger WHERE timestamp LIKE \'2026-07-13%\' ORDER BY timestamp DESC LIMIT 10'); print([dict(r) for r in c.fetchall()])
