services:
  - type: web
    name: sovereign-alpha
    env: python
    region: singapore
    plan: free
    buildCommand: >
      pip install -r requirements-render.txt
    startCommand: python dashboard/app.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: FLASK_ENV
        value: production
